import { DateYearMonthPipe } from './date-year-month.pipe';

describe('DateYearMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new DateYearMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
